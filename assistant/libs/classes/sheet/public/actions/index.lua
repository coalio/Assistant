-- sheet.lua
-- Sheet data action functions

return {
  select      = require(BASE(..., 'select')),
  reference   = require(BASE(..., 'select')),
  min         = require(BASE(..., 'select')),
  set         = require(BASE(..., 'set')),
} 